# Allow mixed-source queues

A Queue may contain Playables from multiple Music Sources, with each item resolved by its source as it approaches playback. Version one naturally contains only Spotify items, but keeping the central Playback Session source-neutral prevents adding the YouTube Source from turning source selection into destructive queue replacement.
